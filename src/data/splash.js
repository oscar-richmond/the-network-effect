import { asset } from '../utils/asset.js';

/** Splash / loading screen image. */
export const splashImage = {
  src: asset('/assets/images/splash/splash.png'),
  width: 1024,
  height: 683,
  alt: 'The Network Effect',
};
