---
/**
 * MOST BRANDS STRUGGLE… — the phone's four lines (mobile rebuild Part 3 §3,
 * 2026-09-08; frame 1:338): 32/30 on a 34 pitch at the frame's indents.
 * Same words as the desktop's three lines (statements.js).
 */
import { CLOSING_HEADLINE } from '../../data/landing/statements.js';
---
<h2 class="m-brands-hl" data-brands-hl-m>
  {CLOSING_HEADLINE.mobile.map((l) => (
    <span class="m-brands-hl__line" style={`--m-x:${l.x}px`}>{l.t}</span>
  ))}
</h2>
